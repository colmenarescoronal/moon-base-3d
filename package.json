{
  "name": "moon-base-tranquility",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "test": "node --test",
    "profile:collisions": "node scripts/profile-collisions.js",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
